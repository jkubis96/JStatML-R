% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statML-R.R
\name{two_groups_analysis}
\alias{two_groups_analysis}
\title{Two-group statistical analysis and Visualization}
\usage{
two_groups_analysis(
  value_column,
  grouping_column,
  data,
  bar_queue = NaN,
  x_label = "",
  x_angle = 30,
  y_label = "",
  size = 10,
  bar_size = 0.5,
  parametric = FALSE,
  paired = FALSE,
  bars = "sem",
  bars_size = 1,
  stat_plot_ratio = 0.15,
  y_break = NaN,
  brew_colors = "Dark2"
)
}
\arguments{
\item{value_column}{A string. Name of the column containing the numeric values to be analyzed.}

\item{grouping_column}{A string. Name of the column indicating group membership (must contain exactly two unique values).}

\item{data}{A data frame containing the data.}

\item{bar_queue}{Optional character vector. Specifies the desired order of the groups on the x-axis. Must match group names.}

\item{x_label}{A string for the x-axis label. Default is an empty string.}

\item{x_angle}{Numeric. Rotation angle of x-axis labels (in degrees). Default is 30.}

\item{y_label}{A string for the y-axis label. Default is an empty string.}

\item{size}{Numeric. Font size for axis labels. Default is 10.}

\item{bar_size}{Numeric. Width of bars in bar and box plots. Default is 0.5.}

\item{parametric}{Logical. If \code{TRUE}, a t-test is performed; if \code{FALSE}, a Wilcoxon/Mann-Whitney test is used.}

\item{paired}{Logical. Whether to perform a paired test. Applies to both parametric and non-parametric tests.}

\item{bars}{Character. Type of error bars: either \code{"sem"} or \code{"sd"} for standard error or standard deviation. Default is \code{"sem"}.}

\item{bars_size}{Numeric. Size (thickness) of error bars. Default is 1.}

\item{stat_plot_ratio}{Numeric between 0 and 1. Proportion of plot height reserved for displaying significance annotations. Default is 0.15.}

\item{y_break}{Optional numeric. Interval for y-axis tick marks. Default is \code{NaN} (automatic).}

\item{brew_colors}{Character. RColorBrewer palette name. Default is \code{"Dark2"}.}
}
\value{
An S4 object of class \code{two_groups_analysis}, containing:
\itemize{
  \item \code{violin_plot} — Violin plot with overlaid statistics.
  \item \code{bar_plot} — Bar plot with error bars.
  \item \code{box_plot} — Box plot with optional statistical annotations.
  \item \code{statistic_tests} — An S4 object from \code{test_two_groups()}, with test details.
  \item \code{statistic_data} — A summary data frame with group-wise statistics.
  \item \code{statistic_txt_resum} — Character summary of the statistical test result.
  \item \code{avg_FC_results} — Output of \code{avg_FC()}, representing average fold change results.
}
}
\description{
This function performs statistical analysis and generates summary plots (violin, bar, and box plots)
for comparisons between two groups. It supports both parametric (t-test) and non-parametric (Wilcoxon/Mann-Whitney) tests,
and can handle paired data.
}
\details{
This function is designed for exploratory visualization and analysis of two-group comparisons.
If more than two groups are present in the grouping column, an error is thrown and the user is advised to use \code{\link{multi_groups_analysis}}.
}
\examples{
df <- data.frame(
  group = rep(c("Control", "Treatment"), each = 10),
  value = c(rnorm(10, 5), rnorm(10, 7))
)
result <- two_groups_analysis(value_column = "value", grouping_column = "group", data = df)
result@bar_plot  # View bar plot
result@statistic_txt_resum  # Summary of the statistical test
}
