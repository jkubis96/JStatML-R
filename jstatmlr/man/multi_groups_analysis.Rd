% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statML-R.R
\name{multi_groups_analysis}
\alias{multi_groups_analysis}
\title{Multiple Group Statistical Analysis and Visualization}
\usage{
multi_groups_analysis(
  value_column,
  grouping_column,
  data,
  bar_queue = NaN,
  x_label = "",
  x_angle = 30,
  y_label = "",
  size = 10,
  bar_size = 0.5,
  parametric = FALSE,
  paired = FALSE,
  include_ns = FALSE,
  bars = "sem",
  bars_size = 1,
  adjustment.method = "bonferroni",
  stat_plot_ratio = 0.2,
  stat_hight = 10,
  y_break = NaN,
  brew_colors = "Dark2"
)
}
\arguments{
\item{value_column}{Character. Name of the column in `data` representing numeric values to be compared.}

\item{grouping_column}{Character. Name of the column in `data` representing grouping factor.}

\item{data}{Data frame. The dataset containing `value_column` and `grouping_column`.}

\item{bar_queue}{Optional character vector. Custom order of groups for plotting (must match group names).}

\item{x_label}{Character. Label for the x-axis.}

\item{x_angle}{Numeric. Angle for x-axis text labels (default = 30).}

\item{y_label}{Character. Label for the y-axis.}

\item{size}{Numeric. Font size for axis titles (default = 10).}

\item{bar_size}{Numeric. Width of the bars and boxes (default = 0.5).}

\item{parametric}{Logical. If TRUE, uses parametric tests (ANOVA + t-test); if FALSE, uses Kruskal-Wallis + Wilcoxon / Mann-Whitney test.}

\item{paired}{Logical. If TRUE, assumes paired samples (only for parametric).}

\item{include_ns}{Logical. If FALSE, excludes non-significant comparisons from plot.}

\item{bars}{Character. Error bars type: `"sem"` (default) or `"sd"`.}

\item{bars_size}{Numeric. Thickness of error bars (not currently used).}

\item{adjustment.method}{Character. Method for p-value adjustment in post-hoc test (e.g., `"bonferroni"`, `"holm"`).}

\item{stat_plot_ratio}{Numeric. Relative height of significance plot above main plot (default = 0.2).}

\item{stat_hight}{Numeric. Controls spacing between significance brackets (default = 10).}

\item{y_break}{Numeric. Break interval for y-axis. If NA, default ggplot scale is used.}

\item{brew_colors}{Character. RColorBrewer palette name (default = `"Dark2"`).}
}
\value{
An S4 object of class `multi_groups_analysis` with the following slots:
\describe{
  \item{violin_plot}{ggplot object — violin plot with error bars.}
  \item{bar_plot}{ggplot object — bar plot with error bars.}
  \item{box_plot}{ggplot object — box plot with error bars.}
  \item{statistic_tests}{S4 object — result of group tests (ANOVA/KW + post-hoc).}
  \item{statistic_data}{data.frame — summary statistics (mean, SEM, SD) for each group.}
  \item{statistic_txt_resum}{Character — textual summary of statistical results.}
  \item{avg_FC_results}{data.frame — fold-change comparisons between groups.}
}
}
\description{
Performs statistical analysis for more than two groups, including parametric or non-parametric tests, post-hoc comparisons, and generates bar, box, and violin plots with error bars and significance annotations.
}
\details{
This function is designed for exploratory data analysis involving multiple groups. It automatically handles appropriate statistical tests based on the `parametric` and `paired` flags, formats plots, and annotates p-values with asterisks.

If only 2 groups are detected, an error is returned recommending the use of `two_groups_analysis()`.
}
\examples{
result <- multi_groups_analysis(
  value_column = "Score",
  grouping_column = "Group",
  data = my_data,
  parametric = TRUE,
  bar_queue = c("Control", "Treatment1", "Treatment2"),
  x_label = "Groups",
  y_label = "Response",
  include_ns = FALSE
)
result@bar_plot
result@statistic_txt_resum

}
